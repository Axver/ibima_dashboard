<?php

if (!defined('BASEPATH'))
    exit('No direct script access allowed');

class Question_image extends CI_Controller
{
    function __construct()
    {
        parent::__construct();
        $this->load->model('Question_image_model');
        $this->load->library('form_validation');
    }

    public function index()
    {
        $q = urldecode($this->input->get('q', TRUE));
        $start = intval($this->input->get('start'));
        
        if ($q <> '') {
            $config['base_url'] = base_url() . 'question_image/index.html?q=' . urlencode($q);
            $config['first_url'] = base_url() . 'question_image/index.html?q=' . urlencode($q);
        } else {
            $config['base_url'] = base_url() . 'question_image/index.html';
            $config['first_url'] = base_url() . 'question_image/index.html';
        }

        $config['per_page'] = 10;
        $config['page_query_string'] = TRUE;
        $config['total_rows'] = $this->Question_image_model->total_rows($q);
        $question_image = $this->Question_image_model->get_limit_data($config['per_page'], $start, $q);

        $this->load->library('pagination');
        $this->pagination->initialize($config);

        $data = array(
            'question_image_data' => $question_image,
            'q' => $q,
            'pagination' => $this->pagination->create_links(),
            'total_rows' => $config['total_rows'],
            'start' => $start,
        );
        $this->load->view('question_image/question_image_list', $data);
    }

    public function read($id) 
    {
        $row = $this->Question_image_model->get_by_id($id);
        if ($row) {
            $data = array(
		'image' => $row->image,
		'id' => $row->id,
	    );
            $this->load->view('question_image/question_image_read', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('question_image'));
        }
    }

    public function create() 
    {
        $data = array(
            'button' => 'Create',
            'action' => site_url('question_image/create_action'),
	    'image' => set_value('image'),
	    'id' => set_value('id'),
	);
        $this->load->view('question_image/question_image_form', $data);
    }
    
    public function create_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->create();
        } else {
            $data = array(
	    );

            $this->Question_image_model->insert($data);
            $this->session->set_flashdata('message', 'Create Record Success');
            redirect(site_url('question_image'));
        }
    }
    
    public function update($id) 
    {
        $row = $this->Question_image_model->get_by_id($id);

        if ($row) {
            $data = array(
                'button' => 'Update',
                'action' => site_url('question_image/update_action'),
		'image' => set_value('image', $row->image),
		'id' => set_value('id', $row->id),
	    );
            $this->load->view('question_image/question_image_form', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('question_image'));
        }
    }
    
    public function update_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->update($this->input->post('image', TRUE));
        } else {
            $data = array(
	    );

            $this->Question_image_model->update($this->input->post('image', TRUE), $data);
            $this->session->set_flashdata('message', 'Update Record Success');
            redirect(site_url('question_image'));
        }
    }
    
    public function delete($id) 
    {
        $row = $this->Question_image_model->get_by_id($id);

        if ($row) {
            $this->Question_image_model->delete($id);
            $this->session->set_flashdata('message', 'Delete Record Success');
            redirect(site_url('question_image'));
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('question_image'));
        }
    }

    public function _rules() 
    {

	$this->form_validation->set_rules('image', 'image', 'trim');
	$this->form_validation->set_error_delimiters('<span class="text-danger">', '</span>');
    }

}

/* End of file Question_image.php */
/* Location: ./application/controllers/Question_image.php */
/* Please DO NOT modify this information : */
/* Generated by Harviacode Codeigniter CRUD Generator 2020-07-04 02:28:51 */
/* http://harviacode.com */